// TODO: replace with real values from the logged-in user once auth is
// wired in (e.g. from a useAuth() hook or session context). These are
// numbers because branch_id/user_id are Int columns in the database.
export const CURRENT_BRANCH_ID = 1;
export const CURRENT_USER_ID = 1;